info:
  name: "Snapshot probe"
  type: http
  seq: 21

http:
  method: GET
  url: "http://{{device_host}}/ISAPI/Streaming/channels/{{track_stream_id}}/picture"
  auth:
    type: digest
    username: "{{username}}"
    password: "{{password}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
