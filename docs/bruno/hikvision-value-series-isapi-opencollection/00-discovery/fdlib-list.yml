info:
  name: "FDLib list"
  type: http
  seq: 9

http:
  method: GET
  url: "http://{{device_host}}/ISAPI/Intelligent/FDLib"
  params:
    - name: "format"
      value: "json"
      type: query
  auth:
    type: digest
    username: "{{username}}"
    password: "{{password}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
