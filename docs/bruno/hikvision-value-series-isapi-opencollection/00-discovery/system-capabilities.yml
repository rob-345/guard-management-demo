info:
  name: "System capabilities"
  type: http
  seq: 2

http:
  method: GET
  url: "http://{{device_host}}/ISAPI/System/capabilities"
  auth:
    type: digest
    username: "{{username}}"
    password: "{{password}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
