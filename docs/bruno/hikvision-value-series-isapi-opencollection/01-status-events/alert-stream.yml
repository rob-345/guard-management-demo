info:
  name: "Alert stream"
  type: http
  seq: 17

http:
  method: GET
  url: "http://{{device_host}}/ISAPI/Event/notification/alertStream"
  auth:
    type: digest
    username: "{{username}}"
    password: "{{password}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
